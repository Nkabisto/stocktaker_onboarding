// When user views calendar
async function getAvailableSlots(startDate, endDate){
  // Check if slots exist for date range
  1. How to loop through dates
  =============================
  let currentDate = startDate;
  while(currentDate < endDate){
    //...do something;
    currentDate.setDate(currentDate.getDate() + 1); // iterate to next day
  }

  2.How to check for weekends
  ============================
  const isWeekend = (currentDate) =>{
    const dayOfWeek = currentDate.getDay();
    return dayOfWeek ===0 || dayOfWeek === 6;

  3. How to check for holidays
  =============================
  //  An sql query in the pre-populated table black-outs (need to be renamed to unavailable_days) to check if startDate or endDate are present
  //  SELECT date FROM black_outs 
  //  WHERE date = startDate || date = endDate 
  //  AND reason = 'Public holiday'
  //  if startDate is found return true else return false

  4. How to avoid duplicates
  ==============================
  // The event_occurences table contains validations that check that the template_id column and the start_time column are unique
